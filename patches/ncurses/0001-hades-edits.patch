diff -Naur ../ncurses-v6.2-orig/config.sub ./config.sub
--- ../ncurses-v6.2-orig/config.sub	2025-01-02 18:47:35.619725077 -0500
+++ ./config.sub	2025-01-02 18:47:45.451668686 -0500
@@ -1366,7 +1366,7 @@
 	     | skyos* | haiku* | rdos* | toppers* | drops* | es* \
 	     | onefs* | tirtos* | phoenix* | fuchsia* | redox* | bme* \
 	     | midnightbsd* | amdhsa* | unleashed* | emscripten* | wasi* \
-	     | nsk* | powerunix)
+	     | nsk* | powerunix | hades* )
 	# Remember, each alternative MUST END IN *, to match a version number.
 		;;
 	qnx*)
diff -Naur ../ncurses-v6.2-orig/configure ./configure
--- ../ncurses-v6.2-orig/configure	2025-01-02 18:47:35.623725054 -0500
+++ ./configure	2025-01-02 18:47:43.099682219 -0500
@@ -5961,6 +5961,10 @@
 
 		MK_SHARED_LIB='${CC} ${LDFLAGS} ${CFLAGS} -shared -Wl,-soname,'$cf_cv_shared_soname',-stats,-lc -o $@'
 		;;
+    (hades*)
+        CC_SHARED_OPTS='-fPIC'
+        MK_SHARED_LIB='${CC} -shared -o $@'
+        ;;
 	(mingw*)
 		cf_cv_shlib_version=mingw
 		cf_cv_shlib_version_infix=mingw
diff -Naur ../ncurses-v6.2-orig/test/picsmap.c ./test/picsmap.c
--- ../ncurses-v6.2-orig/test/picsmap.c	2025-01-02 18:47:35.679724734 -0500
+++ ./test/picsmap.c	2025-01-02 18:47:39.883700679 -0500
@@ -60,6 +60,8 @@
 #define my_intptr_t	long
 #endif
 
+#include <strings.h>
+
 #if HAVE_TSEARCH
 #include <search.h>
 #endif
